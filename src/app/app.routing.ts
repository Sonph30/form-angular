import { Route } from "@angular/router";


export const appRoutes: Route[] = [
    // {path: 'dashboards', pathMatch : 'full', redirectTo: 'dashboards',
    // children: [
    //     {path: 'loginloguot', loadChildren: () => import('./loginloguot/loginloguot.module').then(m => m.LoginlogoutModule)},
        
    // ]},
];